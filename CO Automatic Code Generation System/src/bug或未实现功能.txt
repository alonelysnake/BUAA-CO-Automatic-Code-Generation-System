jalr先不实现
如果指令没有成功创建，应该重新随机一遍
beq指令极其容易出现死循环（现在改成了offset设为恒正，但会测试不全（无法检验offset是否为有符号扩展）），50%左右概率一定会跳转（恒等跳转）

j指令和beq指令只能二选一（目前改进：把j end 设置成beq的结尾）
j指令穿插在代码之间（根据具体指令名决定在哪出现）
beq指令的imm值与实际生成代码不符，需要修改

jr指令无法检测非jr 31的指令（可尝试方案：一个代码块默认jr 31，另一个先把31赋值给一个可用寄存器然后再jr？）

已经实现的指令：
addu    subu    ori     lui     lw      sw      jal     jr      j