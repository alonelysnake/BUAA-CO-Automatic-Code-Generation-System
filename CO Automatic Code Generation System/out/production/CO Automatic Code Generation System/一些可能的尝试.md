一些尚未确定是否实现的功能
--
固定几个寄存器存储边界值？（已经实现：$2：2^32-1 $3：-2^32）

指令是否要测试完全？

简化addI部分的代码
***
对涉及跳转指令的可能处理方式
--
1.所有指令跳转到最后生成label的区域，并以beq 0 0 end结束（jal用jr ra），每个label块内部运行不涉及跳转（目前逻辑）

缺点：未发生跳转的指令可以检测多个，发生的只能检测一个，只能检测正跳转

2.同1，label块内处理：label0作为负跳转时的标签，I类分支先正条再负跳（感觉可以尝试）

缺点：未发生跳转的指令可以检测多个，发生的只能检测一个

目前进行：

3.同1，先负跳再正跳，比如：beq正跳到1，然后负跳到0，然后用下一个分支指令正跳到3，负跳到2，再下一个分支指令正跳到4……（跳转量会较大，但很值得一试）

实现约定：偶数位为实现完负跳转，要进行正跳转，主函数全部分支都是到奇数label

参考模板：

beq $0, $0, label3

label2:

beq $0, $0, label4

label3:

beq $0, $0, label2

label4:

bne...
***